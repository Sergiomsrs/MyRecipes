# Agents

## Compilación y tests

Las compilaciones y pruebas de funcionamiento se realizan de forma manual por el desarrollador. No ejecutar `mvn compile`, `mvn test` ni comandos similares automáticamente.
